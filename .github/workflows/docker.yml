name: ci

on: 
  push:
    branches: master

jobs:
  test:
    name: Build and push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2

      - name: Build image
        run:
          Docker build .
           
